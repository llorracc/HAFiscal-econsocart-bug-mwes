#!/bin/bash
# Binder post-build script
# Sets up the LaTeX environment for econsocart bug demonstrations

set -ex

# Make compile scripts executable
chmod +x font-shape-bug/compile.sh headers-draft-bug/compile.sh

# Quick verification (non-blocking)
pdflatex --version | head -1 || true
echo "Environment ready - see Interactive-Bug-Demonstrations-With-Workarounds.ipynb"

